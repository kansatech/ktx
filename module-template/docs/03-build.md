# Build

Document the exact build-host commands required to produce the module image from `image/`.

Include pinned upstream versions, checksums/signatures where applicable, image tag format, and validation commands.
