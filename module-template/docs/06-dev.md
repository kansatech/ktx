# Dev Deployment and Test

Document how the exact build artifact is imported/deployed to `ktx-dev-26`, how a representative instance is created, and the acceptance tests required before production promotion.
