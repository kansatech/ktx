# Configuration

Document every non-secret configuration input and every secret input.

State where generated instance configuration belongs under `/srv/ktx/containers`, persistent data under `/srv/ktx/data`, and secrets under `/srv/ktx/secrets`.
