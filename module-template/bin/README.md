# Module Commands

Module-owned helper commands belong here, for example:

- `build`
- `validate`
- `instantiate`
- `export`

Keep host-global behavior in `Kansatech/ktx`; keep module-specific behavior in the module repository.
