schema: 1
name: REPLACE_MODULE
repository: Kansatech/ktx-REPLACE_MODULE
image: ktx/REPLACE_MODULE
version: 0.1.0

network:
  primary_ip: ".2"  # Offset from the subnet base; use net show's actual primary address.

ports: []

ingress:
  web: false
  ssh: false

persistent_state: []

notes: []
