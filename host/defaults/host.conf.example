# Non-shell key=value settings; examples are deliberately fake.
# Select a non-overlapping pool before creating any networks; do not change it later.
KTX_ENV=prod
KTX_HOST=prod.example.invalid
KTX_DOCKER_POOL=172.28.0.0/16
KTX_NETWORK_PREFIX=ktx-net-
KTX_ACME_EMAIL=admin@example.com
