# SSHPiper User Template
Copy using the external login name. Add `authorized_keys`, gateway upstream private key required by the workingdir plugin, and `known_hosts`. Protect routing/auth files from group/other access. Use strict host-key validation.
