# Restore Testing

> **Purpose:** Turn backups into demonstrated recovery capability.

Monthly: choose a site, restore files to temp, import DB dump to temp/dev DB, run with recorded runtime, verify basics.

Quarterly: bootstrap a disposable host/dev, import platform release, restore multiple sites, recreate networks, validate proxy/DB/mail/SSH.

Record date, snapshot, site, duration, errors, missing assumptions and handbook corrections.
