# ktx-dev-26

> **Purpose:** Everything deliberately looser in development.

May include Xdebug, private MySQL host mappings (33601+), test SSH accounts, staging ACME/test domains, and sanitized copies of data.

Mail should be restricted to safe destinations/test provider. Dev imports exact build artifacts and proves them; it does not silently rebuild them.
